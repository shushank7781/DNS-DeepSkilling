created a logger class using singleton design pattern 
concepts that i used are - static instance variable 
                         - getinstance() method
made the constructor private  to prevent external object creation 
i used static instance variable to store the single object
output that i got - only one instance of logger is created and used across the application 
                         
                      
