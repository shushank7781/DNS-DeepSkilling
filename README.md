-created a logger class using singleton design pattern 
concepts that i used are - static instance variable 
                         - getinstance() method
made the constructor private  to prevent external object creation 
i used static instance variable to store the single object
output that i got - only one instance of logger is created and used across the application 
-implemented the factory method design pattern by creating a document interface and concrete document class such as word document,pdf document 
-created a product class and implemented both linear search and binary search.linear search scans one by one 
-implemented financial forecasting system using recursion ,predicts future values by repeatedly applying a growth rate for a given number of years,base case stops the recursion when the year count becomes zero 
-used PL/SQL control structures such as FOR LOOPS and IF condition to automate banking operation ,the program promotes high balance customers to status and generates remindersfor loan due within 30 days 
-implemented three PL/SQL procedures for banking operations , the first procedures processed monthly interest for savings accounts by applying a 1% increase to balances.the second procedure updated employee salaries by applying a bonus precentage to a specified department, the third procedure transferred funds between accounts after validating sufficient balance 
- set up  the junit framework in a java project to enable automated unit testing,i created a simple calculator class containing an add() method and a calculator test class using junit's @test annototaion and assert equals
- used jnuit assertions for automated unit testing,created a test class and wrote a test method using the @test annotation ,used different assertion methods such as assert equals() assert  true() aseert false() assert null() assertnot null()                        
                      
